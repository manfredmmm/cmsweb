= form_for [@teacher, @web_space], :validate => true do |f|
  = f.label :name, "Nom espai"
  = f.text_field :name

  = f.label :content, "Descripcio"
  = f.text_field :content

  %div.actions
    = f.submit "Actualitzar", :class => "btn btn-primary"
