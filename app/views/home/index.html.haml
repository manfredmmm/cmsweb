- unless @search.nil?
  %h1 Resultats de la búsqueda

  #search_bar
    %h3 Professors
    - if @search.empty?
      %p No hi ha resultats de professors amb la búsqueda realitzada.
    - else
      %ul
      - @search.each do |s|
        %li
          = image_tag(s.avatar_url.to_s, :title => s.name + " " + s.surname, :alt => s.name + " " + s.surname)
          = link_to "#{s.name} #{s.surname}", teacher_path(s)
    %h3 Espais web
    - if @web_search.empty?
      %p No hi ha resultats d'espais webs amb la búsqueda realitzada.
    - else
      %ul
        - @web_search.each do |w|
          %li
            =link_to "#{w.name}", teacher_web_space_page_path(w.teacher.id,w,w.pages.first)
            = " ("
            =link_to "#{w.teacher.name} #{w.teacher.surname}", teacher_path(w.teacher.id)
            = ")"

- else
  %h1= t('.title')

  #webspaces
    %h3= t('.webspaces')
    %ul
    - @webspaces.each do |espai|
      #web{:id => espai.name}
        %li
          .web= link_to espai.name, teacher_web_space_page_path(espai.teacher, espai, espai.pages.first)
          .avatar= image_tag espai.teacher.avatar_url
          .teacher= link_to "#{espai.teacher.name} " "  #{espai.teacher.surname}", teacher_path(espai.teacher)
          .date= " [ #{espai.updated_at.strftime('%d/%m/%Y')} ]"

  #teachers
    %h3= t('.latest')
    %ul
    - @teachers.each do |teacher|
      #teacher{:id => teacher.name}
        %li
          .avatar= image_tag teacher.avatar_url
          .teacher= link_to("#{teacher.name} #{teacher.surname}", teacher_path(teacher))
          .date= " [ " + teacher.created_at.strftime('%d/%m/%Y').to_s + " ]"
